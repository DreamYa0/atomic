package com.atomic.config;

public class GlobalModeHolder {

    private GlobalMode globalMode;

    public GlobalMode getGlobalMode() {
        return globalMode;
    }

    public void setGlobalMode(GlobalMode globalMode) {
        this.globalMode = globalMode;
    }
}
